# Core dependencies: text/PDF/DOCX/XLSX extraction.
# The full ML stack (torch, openai-whisper, easyocr, transformers) is in
# requirements.txt; it needs a native build environment and a CUDA-capable
# image. main.py degrades gracefully when those modules are absent.
fastapi==0.115.0
uvicorn[standard]==0.32.0
python-magic==0.4.27
python-multipart==0.0.12
PyMuPDF==1.24.11
python-docx==1.1.2
olefile==0.47
openpyxl==3.1.5
pandas==2.2.3
numpy==1.26.4
pillow==11.0.0
